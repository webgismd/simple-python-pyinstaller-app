pipeline {
     agent  kubernetes 
    stages {
        stage('build') {
            steps {
                sh  'pwd'
                sh 'python --version'
                
            }
        }
    }
}
