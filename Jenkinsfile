pipeline {
     agent { kubernetes {'python'} }
    stages {
        stage('build') {
            steps {
                sh  'pwd'
                sh 'python --version'
                
            }
        }
    }
}
