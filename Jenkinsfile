pipeline {
    agent kubernetes
    stages {
        stage('build') {
            steps {
container('python') {
    sh 'python --version'}
              
                
            }
        }
    }
}
